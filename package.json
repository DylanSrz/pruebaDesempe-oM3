{
  "name": "cineapp",
  "private": true,
  "version": "1.0.0",
  "description": "Cinema showtimes and ticket booking SPA built with Vanilla JS + Vite",
  "type": "module",
  "scripts": {
    "start": "concurrently -n api,web -c blue,green \"npm:api\" \"npm:dev\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "json-server": "^0.17.4",
    "vite": "^5.4.0"
  }
}
